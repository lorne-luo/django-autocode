============
Installation
============

At the command line::

    $ easy_install django-autocode

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-autocode
    $ pip install django-autocode
